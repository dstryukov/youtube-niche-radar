# Frontend placeholder

Minimal Next.js dashboard for MVP validation.

```bash
cd frontend
npm install
NEXT_PUBLIC_API_BASE=http://localhost:8000 npm run dev
```

Screens implemented:
- summary cards from `/dashboard/summary`
- top outliers from `/videos/outliers`

Next UI tasks:
- add channel form
- CSV upload
- task queue status
- filters by niche, format, faceless, AI-friendly, repeatability
